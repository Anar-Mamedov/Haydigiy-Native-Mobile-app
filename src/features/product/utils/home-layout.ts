/**
 * Gap (px) between home page design sections: between rows, side by side, and
 * between images stacked in one banner section. Kept equal to the web mobile
 * home (`gap-[5px]` in the frontend's MobileHome) so the app and site match.
 */
export const HOME_SECTION_GAP = 5;
